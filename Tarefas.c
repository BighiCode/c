#include "Tarefas.h"
/*
int lerArquivo()
{
    FILE* arqTarefas;
    arqTarefas = fopen("tarefas.dat", "r");
    if (arqTarefas == NULL)
    {
        printf("Erro ao abrir o arquivo");
        return 0;       
    }else{
        int i = 0;
        while(fscanf(arqTarefas, "%d %d %d %d %d %d %d" , ) == 7){
            i++;
        }
        fclose(arqTarefas);
        return i;
    }
}
    */
